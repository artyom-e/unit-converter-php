includes:
    - phpstan-baseline.neon

parameters:

    paths:
        - src

    # The level 9 is the highest level
    level: 8

    ignoreErrors:

    excludePaths:

    editorUrl: 'phpstorm://open?file=%%file%%&line=%%line%%'
